# Database (Railway PostgreSQL)
DATABASE_URL=postgresql://postgres:password@postgres-service.railway.internal:5432/railway

# JWT Secret (vygenerovaný)
JWT_SECRET=a83b3c913e39cd1976099a1f724c938c4ab08e9eaf2dda901929086fcc030a9f

# Redis (přidat Redis service v Railway)
REDIS_URL=redis://default:password@redis-service.railway.internal:6379

# Environment
NODE_ENV=development

# Server Ports (Apollo Server 4 Standalone + Fastify REST API)
REST_PORT=3000      # Fastify REST API (health checks, db-test)
GRAPHQL_PORT=4000   # Apollo Server 4 Standalone (GraphQL API)
HOST=localhost      # Listen interface (localhost pro development, 0.0.0.0 pro production)

# === PŘIPRAVIT POZDĚJI ===

# Comgate V2 (až budete mít web a schválený účet)
COMGATE_SECRET_KEY=placeholder_comgate_key
COMGATE_MERCHANT_ID=placeholder_merchant_id

# Email (až budete mít SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=placeholder@gmail.com
SMTP_PASS=placeholder_app_password

# Pro development můžete použít Mailtrap
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USER=mailtrap_user
# SMTP_PASS=mailtrap_pass 